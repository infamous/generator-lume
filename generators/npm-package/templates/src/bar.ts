// code can also be written in TypeScript.

export function bar(b: boolean): boolean {
	return b
}

// Leave this last line alone, it gets automatically updated when publishing a
// new version of this package.
export const version = '21.0.6'
